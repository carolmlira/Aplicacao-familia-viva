Encapsulamento

é dos conceitos fundamentos em poo, ele descreve a ideia de agrupar dados e os métodos que manipula, esses dados em uma unidade, isso impõe restrições ao acesso direyo a variáveis e meteodos e pode evitar a modificação acidental de dados, para evitar alterações acidentais a variável de um objeto so pode ser alterada peli método desse objetp


No diagrama de classes é o sianlde menos quando o é privado e ser for publico é com +





modificadores de acesso

em python usa convenções no nome do recurso para definir se a variável é publica ou privada

_ para privado
publico apenas o nome dele

Publico pode ser caessado de fora da classe


Privado so pode ser acessado pela classe

todos os recursos são públicos a mens-os que o nome inicie com _, ou seja o interceptador python nao ira garantir a proteção do recurso mas por ser uma convenção amplamente adotada na comunidade quando encontramos uma variável ou método com nome iniciado por _, sabemos que nao deveríamos manipular o seu valor diretamente, ou invocar o método fora do escopo da classe



exemplo

class Conta:
   def __init__(self, saldo=0):
       self._saldo = saldo

   def depoitar(self, valor):
    slef._saldo += valor

   def sacar(self, valor):
       self._saldo -= valor

nao é recomendado mostrar as variáveis privadas

Propriedade ou property
você pode cirar atributos gerenciados em suas classes, você pode usar atributos gerenciados, tambem conhecidso como propriedades, quando precisar modifivar sua implementação interna sem alterar a APIa publica da classe

@property
ela pega o método e transforma em atributo

@.setter

@.deleter

faça um exemplo de código para cada 



