Herança em poo

em program herança é a capacidade de uma classe filha derivar ou herdar as carcteristicas e comportamentos da classe pai(base)

benifcios da hrencça
representa bem os relacionamnetos do mundo rral

fornece reautilização de cidigo, nao precismos escrever o mesmo codig repetidamente, além disso permite adicionar mais recursos a uma classe sem modificala

é de natureza transtitiva que significa se a calsse b herdar da classe a, tosas as subclasses de b hderdarao automaticamente da class a

sinteaxe da herenã

class A:
   pass

class B(A)


herança simples ou herança unica

herança simples é quando a classe filha herda apenas uma classe pai

class Veiculo:
   def __init__(self, cor, placa, numero rodas):
       self.cor = cor
       self.placa = placa
       self.numero_placa = numero roda

   def ligar motor(self):
       print("LIgando o moto")


class Motocicleta(Veiculo)
   pass
class Carro(Veiculo)
   pass


class Caminhão(Veiculo)
   def esta carregada(self):
       print("Nao etsa carregado") #declarar uma coisa especifica mesmo sendo da classe pai, nas outras como motocicleta e carro nao foi adicionanad no seu escopo

moto = Motocicleta("preta", "abc-1234", 2)
moto.ligar_motor()

carro = Carro("branco", "abc-4321", 4)
carro.ligar_motor()
  
super().__init__(cor, placa, numero rodas)

herança múltipla

quando uma classe filha herda de varias classes pai


explica a tag super.().__init__()
tag .mro e __mro__

exemplo
class A:
   pass

class B:
   pass

class C(A, B):
   pass

