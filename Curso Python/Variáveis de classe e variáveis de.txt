Variáveis de classe e variáveis de instancia

atributsod de isntancia são diferentes para cada objeto (cada objeto tem uma copia) já os atributos de classe são compaertilhados entre os objetos


faça um exemplo disso em código python


atributos d instancia 
é único para cada objeto, signiffca se por exemplo se mudaar o numero de matricula do aluno 1 nao vai mudar do aluno 2

variáveis de classe

class Estudante:
   escola = "DIO"

métodos de classe
estão ligados a classe e nao ao objeto, els tem acesso ao estado da classe pois recebemum pataemtro que aponta para a classe e nao para intasncia do objeto
cls

métodos estáticos 
nao receb um primeiro argyumenoto explicitio, ele tamebm pe um método vincukado a classe e nao ao objeto da classe, nao pode acessar ou modificar o estado da classe, ele esta presnete em uma classe porque faz sentido que o método esteja presente na classe

diferenças método de classe recebe um primeiro parâmetro que aponta praa a classe enquanto um método estático nao

método de classe pode acessar ou modificar o estado da classe enquanto um estático nao pode acessa-lo ou modifica-lo(pq ele nao tem a referencia para fazer essa modiciação)

quando utilizar método de classe ou estático

geralmente usamos o método de classe para criar método de fabrica
geralmente usamos métodos estáticos para criar funções utilitárias

class Pessoa:
   deff __init__(cls, nome, idade):
       self.nome = nome 

@classmethod

@staticmethod









interfaces e classes abstratas

oq são interfaces 
define oq uma classe deve fazer e nao como

interface em python 
o conecieto de interface é definir um contrato onde ao declarados os metdps (oque deve ser feito) e suas respectividvas aassinaturas, em python utililizamos classes abstratas para crisr contratos, classes abstrats nao podem serr instanciada

compor um comportamento numa classe

classes abstrats com o modulo abc

abstractmethod, python ao fornece classes abstratas porem ele vem com um modeluo que fornece a base para definir as classes abstratas e o nome do modulo é abc, ele funciona deocrando métodos da classe base como absytayps ee ,e seguida regsitardndo classes concretas como implementações da nase abstrata, um método se torna abstrato quando decorado com @abstractmethod


from abc import ABC

class ControleRemoto(ABC):

quer dixer que no código quando utlizia a classe abstrato isso faz com que o codgio tenha uma padronização, quer dixer que todas as classes abstratas obrigatoriamente implementar métodos abstratos 

@property
@abstractproperty
