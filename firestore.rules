rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permitir leituras e gravações apenas para um token específico ou identificador do servidor
      allow read, write: if request.auth.token.isAdmin == true;
    }
  }
}
